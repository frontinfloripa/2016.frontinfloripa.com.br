<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">

    <title>{{ config.title }} • {{ config.description }}</title>

    <!-- CSS -->
    <link href="//fonts.googleapis.com/css?family=Nunito:300,400,700" rel="stylesheet">
    <link rel="stylesheet" href="assets/css/main.css">

    <!-- Favicons -->
    <link rel="apple-touch-icon" sizes="180x180" href="assets/favicons/apple-touch-icon.png">
    <link rel="icon" type="image/png" href="assets/favicons/favicon-32x32.png" sizes="32x32">
    <link rel="icon" type="image/png" href="assets/favicons/favicon-16x16.png" sizes="16x16">
    <link rel="manifest" href="assets/favicons/manifest.json">
    <link rel="mask-icon" href="assets/favicons/safari-pinned-tab.svg" color="#ffffff">
    <meta name="theme-color" content="#ffffff">

    <!-- Facebook -->
    <meta property="og:title" content="{{ config.title }}" /> 
    <meta property="og:type" content="website" /> 
    <meta property="og:url" content="http://frontinfloripa.com.br" />
    <meta property="og:image" content="http://frontinfloripa.com.br/assets/img/fb-cover.jpg" /> 
    <meta property="og:description" content="{{ config.description }}" />
</head>
<body>
    <main class="main">
        <header class="header">
            <div class="header-wrapper">
                <div class="header-content">
                    <h1 class="header-logo">
                        {{ config.title }}
                    </h1>
                    <div class="header-featured">
                        <h2 class="header-featured__title">{{ config.description }}</h2>
                        <p class="header-featured__location">
                            <span class="date">26/Novembro</span> <span class="bull">&bull;</span> <span class="location">Hotel Maria do Mar</span>
                        </p>
                        <div class="header-featured__divisor"></div>
                        <a class="header-button-cta" href="{{ config.tickets.link }}" target="_blank">
                            Inscreva-se
                        </a>
                        <a class="header-button-c4p" href="{{ config.c4p.link }}" target="_blank">
                            Call For Paper
                        </a>
                    </div>
                </div>
            </div>
        </header>

        <section class="section-features">
            <div class="section-wrapper">
                <h2 class="section-title">
                    Front in Floripa
                    <span class="section-subtitle">Porque você não pode perder esse evento?</span>
                </h2>

                <div class="section-features-main">
                    <div class="section-features-item">
                        <div class="section-features-item__icon home"></div>
                        <div class="section-features-item__description">
                            <h3>Nova casa de frente pro mar :)</h3>
                            <p>
                                Depois de 3 edições no Senai, resolvemos mudar! O Hotel Maria do Mar fica bem perto do Senai e além da vista maravilhosa, o auditório comporta 220 pessoas (50 a mais do que as edições anteriores), além de ante-salas próprias para eventos! Vai ser f* =]
                            </p>
                        </div>
                    </div>

                    <div class="section-features-item right">
                        <div class="section-features-item__icon mic"></div>
                        <div class="section-features-item__description">
                            <h3>Melhores front-enders do Brasil</h3>
                            <p>
                                Ao longo dos 3 anos foram escolhidos a dedo os palestrantes. Passou tanta gente foda compartilhando conteúdo e experiência que seria injustiça citar nomes. Sabemos da grandeza do evento, por isso sempre trazemos os melhores desenvolvedores do Brasil, e claro, sempre apoiando a comunidade local =]
                            </p>
                        </div>
                    </div>

                    <div class="section-features-item">
                        <div class="section-features-item__icon comment"></div>
                        <div class="section-features-item__description">
                            <h3>Networking e Pós-evento</h3>
                            <p>
                                O Front in Floripa é considerado por muitos como um dos melhores eventos de Santa Catarina para se fazer networking. Além do evento, procuramos sempre preparar um pós-evento muito bacana para que possa haver uma troca de experiência mais descontraída =]
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section class="section-twitter">
            <ul class="twitter-list" id="twitter">
                {{#each tweets}}
                    <li class="twitter-item">
                        <p class="twitter-item__tweet">
                            {{tweet}}
                        </p>
                        <a href="https://twitter.com/{{username}}" target="_blank" class="twitter-item__metadata">
                            <img class="twitter-item__avatar" src="{{avatar}}">
                            <span class="twitter-item__username">@{{username}}</span>
                        </a>
                    </li>
                {{/each}}
            </ul>
        </section>

        <section class="section-speakers">
            <div class="section-wrapper">
                <h2 class="section-title">
                    Palestrantes
                    <span class="section-subtitle">Os front-enders mais f* do Brasil</span>
                </h2>
                <div class="speakers-list">
                    {{#each speakers}}
                        <div class="speaker-item">
                            <img class="speaker-avatar" src="assets/img/speakers/{{avatar}}">
                            <h3 class="speaker-name">{{name}}</h3>
                            <div class="speaker-social">
                                {{#if github}}
                                    <a class="speaker-social-link github" href="https://github.com/{{github}}" target="_blank"></a>
                                {{/if}}
                                {{#if twitter}}
                                    <a class="speaker-social-link twitter" href="https://twitter.com/{{twitter}}" target="_blank"></a>
                                {{/if}}
                            </div>
                        </div>
                    {{/each}}
                </div>
            </div>
        </section>

        <section class="section-tickets">
            <div class="section-wrapper">
                <div class="tickets-content">
                    <h2 class="tickets-title">
                        R$ 70
                        <span class="tickets-title__line"></span>
                    </h2>
                    <p class="tickets-lot">
                        1º Lote – Válido até 20/11/2016
                    </p>
                    <a class="tickets-buy" href="{{ config.tickets.link }}" target="_blank">Comprar Ingresso</a>
                </div>
            </div>
        </section>

        <section class="section-schedule">
            <div class="section-wrapper">
                <h2 class="section-title">
                    Programação
                    <span class="section-subtitle">Confira abaixo toda a programação do evento!</span>
                </h2>
                <div class="schedule-content">
                    Aguarde...
                </div>
            </div>
        </section>

        <section class="section-location" id="location">
            <div class="section-wrapper">
                <div class="section-location-content">
                    <h2 class="location-title">Hotel Maria do Mar</h2>
                    <p class="location-address">
                        Rodovia João Paulo, 2285<br />
                        Bairro João Paulo – Florianópolis/SC<br />
                        <a class="location-map-link" href="https://goo.gl/n6R7dj" target="_blank">Ver Mapa</a>
                    </p>
                </div>
            </div>
        </section>

        <section class="section-sponsors">
            <div class="section-wrapper">
                <h2 class="section-title">
                    Patrocinadores
                </h2>
                <div class="sponsors-content">
                    {{#each sponsors}}
                        <a class="item" href="{{website}}" target="_blank">
                            <img class="avatar" src="assets/img/sponsors/{{logo}}">
                        </a>
                    {{/each}}
                </div>

                <h2 class="section-title">
                    Apoiadores
                </h2>
                <div class="sponsors-content">
                    {{#each support}}
                        <a class="item apoio" href="{{website}}" target="_blank">
                            <img src="assets/img/support/{{logo}}">
                        </a>
                    {{/each}}
                </div>
            </div>
        </section>

        <footer class="footer">
            <div class="footer-content">
                <p class="footer-mail">contato@frontinfloripa.com.br</p>
                <a class="footer-link" href="https://braziljs.org/coc" target="_blank">Código de Conduta</a>
            </div>
        </footer>
    </main>

    <script>
        (function() {

            var tweets = [].slice.call(document.getElementsByClassName('twitter-item'))
            var location = document.getElementById('location')
            var active = 0
            var total = tweets.length
            var that = this

            var toggleLocation = function() {
                location.classList.toggle('multi')
            }

            var removeAllClasses = function() {
                return tweets.forEach(function(item) {
                    item.classList.remove('active')
                })
            }

            var addActiveClass = function() {
                tweets[active].classList.add('active')
                active = (active === total - 1) ? 0 : active + 1
            }

            var init = function() {
                if (active === 0) {
                    addActiveClass()
                }
                setInterval(function() {
                    removeAllClasses()
                    addActiveClass()
                    toggleLocation()
                }, 7000)
            }

            init()

        })();
    </script>
</body>
</html>
